#include<iostream>
using namespace std;
int main(){
    // int a;
    // float b;
    // double d;
    // char c;
    // char name[200];
    // int x = sizeof(name);

    // cout << x;
    int x, y, sum;
    sum = (x = 30, y = 40, sum = x + y);
    cout << sum;
    return 0;
}